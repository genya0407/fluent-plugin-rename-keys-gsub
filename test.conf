<source>
  @type forward
</source>

<filter **>
  @type rename_keys_gsub

  from .
  to   _
</filter>

<filter **>
  @type rename_keys_gsub

  from /
  to   _
</filter>

<match **>
  @type stdout
</match>
